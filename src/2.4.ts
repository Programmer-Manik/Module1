{
    let roll : number = 1111;
    let names : string = 'mani';
    let isAdmin: boolean = true;
    let x : null = null;
    let y : undefined = undefined;
    let d : number;
        d = 1111;

    //non primitive type start
    // array type 
    let friends : string[] = ['mani','run','raki']
    let friend : number[] = [1, 11, 22,]
    // tuple array type of value 
    let tuple: [string, number, boolean] = ['manik', 111 , true];
    //non primitive type end
}